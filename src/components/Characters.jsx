import { Box } from "@mui/material";
import Character from "./Character";

const Characters = () => {
  return (
    <Box>
      {/* send props to character component */}
      <Character />
    </Box>
  );
};

export default Characters;
